# Backend Environment Variables
# Copy this to .env and update with your values

# Admin token for presenter endpoints (optional but recommended)
ADMIN_TOKEN=change-me-in-production

# Server configuration
HOST=0.0.0.0
PORT=8000

